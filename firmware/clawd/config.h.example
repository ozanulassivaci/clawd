// Copy this file to config.h and fill in real values.
// config.h is gitignored -- never commit real WiFi/MQTT credentials here.
#pragma once

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

#define MQTT_HOST "192.168.1.100"  // your mosquitto broker's LAN IP
#define MQTT_PORT 1883

// Leave both empty ("") if the broker allows anonymous connections.
#define MQTT_USER ""
#define MQTT_PASSWORD ""

#define MQTT_CLIENT_ID "clawd-display"
